kind: Service
apiVersion: v1
metadata:
  name: hive-service
  namespace: default
spec:
  selector:
    app: hive
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  sessionAffinity: ClientIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-deployment
  labels:
    app: hive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hive
  template:
    metadata:
      labels:
        app: hive
    spec:
      imagePullSecrets:
        - name: pipeline-docker-registry
      containers:
        - name: hive
          image: hive
          ports:
            - containerPort: 80