import {render} from '@testing-library/preact';
import React from 'preact/compat';

describe('', () => {
    beforeEach(() => {
        render(<div/>);
    });

    test('', () => {
        expect(true).toBeTruthy();
    });
});