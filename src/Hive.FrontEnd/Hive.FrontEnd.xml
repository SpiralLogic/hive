<Project Sdk="Microsoft.Build.NoTargets/3.5.6">
    <PropertyGroup>
        <Configurations>Debug;Release;Test</Configurations>
        <Platforms>AnyCPU</Platforms>
        <TargetFramework>netstandard2.0</TargetFramework>
    </PropertyGroup>
    <ItemGroup>
        <Content Include="src\**" CopyToOutputDirectory="Never" CopyToPublishDirectory="Never" />
        <Content Include="test\**" CopyToOutputDirectory="Never" CopyToPublishDirectory="Never" />
    </ItemGroup>

    <Target Name="DebugEnsureNodeEnv" BeforeTargets="Build" Condition=" '$(Configuration)' == 'Debug' And !Exists('node_modules') ">
        <!-- Ensure Node.js is installed -->
        <Exec Command="node --version" ContinueOnError="true">
            <Output TaskParameter="ExitCode" PropertyName="ErrorCode" />
        </Exec>
        <Error Condition="'$(ErrorCode)' != '0'" Text="Node.js and pnpm is required to build and run this project. To continue, please install Node.js from https://nodejs.org/, and then restart your command prompt or IDE." />
        <Exec Command="pnpm --version" ContinueOnError="true">
            <Output TaskParameter="ExitCode" PropertyName="ErrorCode" />
        </Exec>
        <Error Condition="'$(ErrorCode)' != '0'" Text="Node.js and pnpm is required to build and run this project. To continue, please install Node.js from https://nodejs.org/, and then restart your command prompt or IDE." />
        <Message Importance="high" Text="Restoring dependencies using 'pnpm'. This may take several minutes..." />
        <Exec Command="pnpm install" />
    </Target>

    <Target BeforeTargets="Build" Name="BuildDev" Condition="'$(Configuration)' == 'Debug'">
        <Exec ConsoleToMSBuild="true" EnvironmentVariables="NODE_ENV=development" Command="pnpm run build:dev">
            <Output TaskParameter="ExitCode" PropertyName="ErrorCode" />
        </Exec>
        <Error Condition="'$(ErrorCode)' != '0'" Text="Failed to build with pnpm" />
    </Target>
    
    <Target BeforeTargets="Build" Name="BuildRelease" Condition="'$(Configuration)' == 'Release'">
        <Exec ConsoleToMSBuild="true" EnvironmentVariables="NODE_ENV=production" Command="pnpm run build">
            <Output TaskParameter="ExitCode" PropertyName="ErrorCode" />
        </Exec>
        <Error Condition="'$(ErrorCode)' != '0'" Text="Failed to build with pnpm" />
    </Target>

    <Target Name="EsbuildWatch" AfterTargets="Build" Condition="'$(DotNetWatchBuild)'=='true'">
        <Exec Command="pnpm esbuild:watch" />
    </Target>
</Project>
